const stages = [['1','React','Chat + evidence graph'],['2','Fastify','SSE, validation, rate limits'],['3','ADK strategy','Gemini grounded answer'],['4','Neo4j','Read-only retrieval + trace'],['5','Explorer','Keyset-paginated graph']];
export function OperationsPage() { return <main className="ops"><p className="eyebrow">PAGE 3 · SYSTEM DESIGN</p><h1>Production architecture</h1><p className="lede">Stateless API replicas. Shared Neo4j Aura + Redis-ready cursor/trace plane. Tenant and policy enforcement before every graph query.</p><section className="flow">{stages.map(([n,t,d]) => <article key={n}><span>{n}</span><h2>{t}</h2><p>{d}</p></article>)}</section><section className="guardrails"><h2>Industrial guardrails</h2><div>{['OIDC SSO + tenant claims','Parameterized Cypher allowlist','Keyset cursor pagination','Depth ≤2 and hard result caps','Driver pool + timeouts','SSE disconnect cancellation','Immutable audit events','OpenTelemetry + SLO alerts','Encrypted secrets + rotation','Backups and restore drills','Canary deploy + rollback','Cost and token budgets'].map(x => <span key={x}>✓ {x}</span>)}</div></section><section className="decision"><h2>Free tier → production</h2><p>AuraDB Free fits demo and schema validation. Industrial workloads need AuraDB Professional/Business Critical or managed cluster: HA, backups, private networking, predictable capacity. Keep driver URI/config portable so application code does not change.</p></section></main>; }

